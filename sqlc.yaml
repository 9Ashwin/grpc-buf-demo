version: "2"
sql:
  - engine: sqlite
    schema: db/migrations
    queries: db/query.sql
    gen:
      go:
        package: db
        out: db
        emit_empty_slices: true
        emit_json_tags: true
